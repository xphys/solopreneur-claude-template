# infra/iac/modules/

Reusable Terraform modules, extracted **when a pattern repeats** — don't pre-build a module
zoo. One folder per module with `main.tf`, `variables.tf`, `outputs.tf`, and a one-paragraph
`README.md` stating what it provisions.
