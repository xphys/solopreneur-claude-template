# Composition root for the prod environment.
# Instantiate modules from ../../modules/ here as the platform's infra takes shape.
# Adopting an existing platform? Start with `import` blocks — see ../../README.md.
