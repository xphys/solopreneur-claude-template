# Flat node_modules — makes the server's transitive deps resolvable regardless of
# pnpm's strict layout, and keeps the launch paths in ../.mcp.json stable.
node-linker=hoisted
# Don't fail install if the package declares a narrower Node range than we run.
engine-strict=false
