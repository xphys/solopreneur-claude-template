# Gotchas

Landmines that will bite an agent (or human) who doesn't know them. **Read before changing
anything; add an entry the moment you get bitten.**

Format — one `##` section per gotcha: what looks true but isn't, what actually happens,
and what to do instead. Date each entry. Delete entries that stop being true.

> Empty on a fresh platform. That won't last.
